{
	"name": "hex",
	"private": true,
	"license": "SEE LICENSE IN LICENSE",
	"scripts": {
		"start": "lerna run --stream --parallel start",
		"build": "lerna run build",
		"build:dev": "lerna run --stream --parallel build:dev",
		"docs": "lerna run --scope @bitnami/hex-docs --stream start",
		"cdn": "gulp cdn"
	},
	"devDependencies": {
		"gulp": "^4.0.1",
		"gulp-s3-upload": "^1.7.3",
		"lerna": "^3.13.4"
	}
}
